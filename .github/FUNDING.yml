# Funding configuration — intentionally inert.
#
# Sill has NO funding accounts yet (no GitHub Sponsors profile, no Open
# Collective). Per project policy (docs/FUNDING.md), placeholders are never
# filled with accounts that don't exist.
#
# [CONFIGURE AFTER SPONSORS ACCOUNT IS ACTIVE] — uncomment the matching line:
# github: [bloxy-studios]
# open_collective: sill
