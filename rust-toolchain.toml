# Pinned Rust toolchain for reproducible development and CI.
# rustup reads this file automatically; contributors get the right version
# on first `cargo` invocation. Bumps are deliberate PRs (see
# docs/DEPENDENCY_POLICY.md).
[toolchain]
channel = "1.98.0"
components = ["rustfmt", "clippy"]
